name: Deploy Website

on:
  push:
    branches: [main]
    paths:
      - "packages/landing/**"
      - "packages/docs/**"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v6
        with:
          node-version-file: ".nvmrc"
          cache: "pnpm"

      - run: pnpm install --frozen-lockfile

      - name: Build landing page
        run: pnpm --filter @itwillsync/landing build

      - name: Build docs
        run: pnpm --filter @itwillsync/docs build

      - name: Merge into single site
        run: cp -r packages/docs/.vitepress/dist packages/landing/dist/docs

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: packages/landing/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
