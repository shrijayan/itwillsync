{
  "name": "itwillsync-root",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @itwillsync/web-client build && pnpm --filter itwillsync build",
    "dev": "pnpm -r --parallel dev",
    "clean": "pnpm -r clean"
  },
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=20.0.0"
  }
}
